/** * Copyright 2026 ??? * * Licensed under the Apache License, Version 2.0 (the "License"); * you may not use this file except in compliance with the License. * You may obtain a copy of the License at * *     http://www.apache.org/licenses/LICENSE-2.0 * * Unless required by applicable law or agreed to in writing, software * distributed under the License is distributed on an "AS IS" BASIS, * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. * See the License for the specific language governing permissions and * limitations under the License. */import { Server } from "@modelcontextprotocol/sdk/server/index.js";import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";import { StreamableHTTPServerTransport } from "@modelcontextprotocol/sdk/server/streamableHttp.js";import { CallToolRequestSchema, ListToolsRequestSchema, ListResourcesRequestSchema, ReadResourceRequestSchema, ErrorCode, McpError } from "@modelcontextprotocol/sdk/types.js";// Ensure DATABASE_URL is set before any Prisma client importprocess.env.DATABASE_URL = process.env.DATABASE_URL || "file:./mcp-cognition.db";import { getPrismaClient, disconnectPrisma } from "./storage/client.js";import { RuleRepo } from "./storage/rule-repo.js";import { DiffLogRepo } from "./storage/diff-log-repo.js";import { ConflictRepo } from "./storage/conflict-repo.js";import { MetricRepo } from "./storage/metric-repo.js";import { handleCaptureDiff } from "./tools/capture-diff.js";import { handleQueryRules } from "./tools/query-rules.js";import { handleConfirmRule } from "./tools/confirm-rule.js";import { handleResolveConflict } from "./tools/resolve-conflict.js";import { handleListRules } from "./tools/list-rules.js";import { handleAnalyzeWorkspace } from "./tools/analyze-workspace.js";import { handleCognitionQuery, handleCognitionValidate, handleCognitionFeedback } from "./tools/cognition-tools.js";import { handleApproveInjection } from "./tools/injection-approval.js";import { handleUpdateConfig } from "./tools/config-tools.js";import { RESOURCES, readCognitionSchema, readCognitionStats, readCognitionDocs } from "./resources/cognition-resources.js";// ?? Zod Schemas (P0 #3) ?????????????????????????????????????import { validateInput, AnalyzeWorkspaceSchema, CaptureDiffSchema, QueryRulesSchema, ConfirmRuleSchema, ResolveConflictSchema, ListRulesSchema, CognitionQuerySchema, CognitionValidateSchema, CognitionFeedbackSchema, ApproveInjectionSchema, UpdateConfigSchema,} from "./schemas/index.js";const server = new Server({ name: "mcp-cognition-engine", version: "1.0.0-alpha.2" }, { capabilities: { tools: {} } });const ruleRepo = new RuleRepo();const diffLogRepo = new DiffLogRepo();const metricRepo = new MetricRepo();const conflictRepo = new ConflictRepo(ruleRepo);async function getMode(): Promise<"silent" | "confirm"> {  const prisma = getPrismaClient();  const config = await prisma.appConfig.findUnique({ where: { id: "default" } });  return (config?.mode as "silent" | "confirm") ?? "silent";}server.setRequestHandler(ListToolsRequestSchema, async () => ({  tools: [    { name: "analyze_workspace", description: "????????????????", inputSchema: { type: "object", properties: { baseCommit: { type: "string" }, headCommit: { type: "string" }, paths: { type: "array", items: { type: "string" } }, taskId: { type: "string" } }, required: ["baseCommit"] } },    { name: "capture_diff", description: "?????????????", inputSchema: { type: "object", properties: { filePath: { type: "string" }, originalContent: { type: "string" }, modifiedContent: { type: "string" }, language: { type: "string" }, projectId: { type: "string" } }, required: ["filePath", "originalContent", "modifiedContent", "language"] } },    { name: "query_rules", description: "??????????????", inputSchema: { type: "object", properties: { language: { type: "string" }, filePath: { type: "string" }, projectId: { type: "string" }, tags: { type: "array", items: { type: "string" } } }, required: ["language", "filePath"] } },    { name: "confirm_rule", description: "??/??/??/??????", inputSchema: { type: "object", properties: { ruleId: { type: "string" }, action: { type: "string", enum: ["accept", "reject", "edit", "skip"] }, editedPattern: { type: "string" }, editedSuggestion: { type: "string" } }, required: ["ruleId", "action"] } },    { name: "resolve_conflict", description: "??????", inputSchema: { type: "object", properties: { conflictId: { type: "string" }, resolution: { type: "string", enum: ["keep_a", "keep_b", "merge", "skip"] }, batchAllSession: { type: "boolean" } }, required: ["conflictId", "resolution"] } },    { name: "list_rules", description: "????", inputSchema: { type: "object", properties: { language: { type: "string" }, scope: { type: "string", enum: ["project", "user", "global"] }, status: { type: "string", enum: ["active", "pending", "archived"] }, projectId: { type: "string" }, limit: { type: "number" }, offset: { type: "number" } } } },  ],}));server.setRequestHandler(CallToolRequestSchema, async (request) => {  const { name, arguments: args } = request.params;  try {    switch (name) {      case "analyze_workspace": { const v = validateInput(AnalyzeWorkspaceSchema, args, name); if (!v.success) return v.error; return await handleAnalyzeWorkspace(v.data as any, ruleRepo, diffLogRepo, metricRepo); }      case "capture_diff": { const v = validateInput(CaptureDiffSchema, args, name); if (!v.success) return v.error; return await handleCaptureDiff(v.data as any, ruleRepo, diffLogRepo, metricRepo, await getMode()); }      case "query_rules": { const v = validateInput(QueryRulesSchema, args, name); if (!v.success) return v.error; return await handleQueryRules(v.data as any, ruleRepo, metricRepo); }      case "confirm_rule": { const v = validateInput(ConfirmRuleSchema, args, name); if (!v.success) return v.error; return await handleConfirmRule(v.data as any, ruleRepo, metricRepo); }      case "resolve_conflict": { const v = validateInput(ResolveConflictSchema, args, name); if (!v.success) return v.error; return await handleResolveConflict(v.data as any, conflictRepo, ruleRepo, metricRepo); }      case "list_rules": { const v = validateInput(ListRulesSchema, args, name); if (!v.success) return v.error; return await handleListRules(v.data as any, ruleRepo); }      case "cognition_query": { const v = validateInput(CognitionQuerySchema, args, name); if (!v.success) return v.error; return await handleCognitionQuery(v.data as any); }      case "cognition_validate": { const v = validateInput(CognitionValidateSchema, args, name); if (!v.success) return v.error; return await handleCognitionValidate(v.data as any); }      case "cognition_feedback": { const v = validateInput(CognitionFeedbackSchema, args, name); if (!v.success) return v.error; return await handleCognitionFeedback(v.data as any); }      case "cognition_approve_injection": { const v = validateInput(ApproveInjectionSchema, args, name); if (!v.success) return v.error; return await handleApproveInjection(v.data as any); }      case "cognition_update_config": { const v = validateInput(UpdateConfigSchema, args, name); if (!v.success) return v.error; return await handleUpdateConfig(v.data as any); }      default: throw new McpError(ErrorCode.MethodNotFound, "Unknown tool: " + name);    }  } catch (err) {    if (err instanceof McpError) throw err;    return { content: [{ type: "text", text: JSON.stringify({ error: String(err) }) }], isError: true };  }});async function main() {  const prisma = getPrismaClient();  // Initialize the SQLite database and push schema  await prisma.$connect();  await prisma.appConfig.upsert({ where: { id: "default" }, update: {}, create: { id: "default", mode: "silent" } });  const transport = new StdioServerTransport();  server.onerror = (err) => console.error("[MCP Error]", err);  process.on("SIGINT", async () => { await disconnectPrisma(); process.exit(0); });  process.on("SIGTERM", async () => { await disconnectPrisma(); process.exit(0); });  await server.connect(transport);  console.error("Agent Tuning Reverse Graph MCP Server running on stdio");}main().catch((err) => { console.error("Fatal error:", err); process.exit(1); });
